# Shared Contracts

These contracts mirror the Pydantic schemas in `backend/app/schemas` and are intended for client use.

For production, generate these from the FastAPI OpenAPI spec to guarantee alignment.
